[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    smoke: fast critical-path checks
    negative: invalid or empty input cases
    positive: valid happy-path cases
pythonpath = .
